# TO DO

allocate -mallc Y
         -mmap 
         -createshared
         -shared

deallocate -malloc
           -mmap
           -shared
           -delkey
           addr

memfill addr 

memdump addr

memory -funcs Y
       -vars Y
       -blocks Y
       -all Y
       -pmap Y

readfile 

writefile

read

write

recurse Y